const FlashMessagesComponent = {
  controller: 'FlashMessagesController',
  templateUrl: '<%= asset_path("components/flashMessages/flashMessages") %>'
}

angular
  .module('makeChangeApp')
  .component('flashMessages', FlashMessagesComponent);