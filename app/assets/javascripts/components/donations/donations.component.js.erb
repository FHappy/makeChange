angular
  .module("makeChangeApp")
  .component("donations", {
    templateUrl: "<%= asset_path('components/donations/donations') %>",
    controller: "DonationsController",
    bindings: {
      charity: '<'
    }
  });